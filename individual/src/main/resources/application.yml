spring:
  application: individual

  redis:
    database: 0
    host: redis.test
    port: 6379
    password:
    timeout: 0
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
  mvc:
    view:
      prefix:
      suffix: .html
  thymeleaf:
    cache: false
hostname: ${HOSTNAME}